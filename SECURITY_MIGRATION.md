# 上线前必须完成的安全改造

当前静态页面直接包含模型 API Key，并使用公开视频 URL。发布下一版本前需要完成以下操作。

## 1. 立即轮换模型 API Key

现有 Key 已出现在公开网页源码中，应在阿里云控制台立即禁用并重新生成。

不要把新 Key 写回 HTML、JavaScript 或 GitHub 仓库。

## 2. 增加服务端 AI 代理

可使用 Supabase Edge Function、Cloudflare Worker 或自有后端：

1. 在服务端环境变量中保存模型 API Key。
2. 浏览器只请求自己的 `/api/chat` 接口。
3. 服务端校验用户身份、频率、请求长度和每日额度。
4. 服务端调用模型 API，并只返回必要结果。
5. 增加超时、重试、日志脱敏和异常监控。

## 3. 将视频存储桶设为私有

1. 将 `interview-videos` Bucket 改为 private。
2. 配置 RLS，只允许记录所有者读写自己的路径。
3. 回放时由服务端或已认证客户端生成短期 signed URL。
4. 增加删除视频和设置保存期限的能力。

## 4. 完善数据治理

1. 明确简历、JD、回答和视频的保存期限。
2. 提供删除本地数据和删除云端数据入口。
3. 在隐私政策中列出实际使用的第三方服务。
4. 商业上线前由专业人士审核隐私政策、用户协议、收费和退款条款。

## 5. 正式构建前端资源

当前页面使用 `cdn.tailwindcss.com`。生产发布前应增加构建流程，输出压缩后的静态 CSS，并添加基础自动化测试。
