# P0 安全改造部署指南

代码已全部改完，但以下 **4 步必须由你亲自操作**，做完之后网站才能正常工作（现在前端已经不带 Key 了，不部署代理 AI 功能会失效）。

---

## 第 1 步：轮换阿里百炼 API Key（最紧急！）

旧 Key `sk-ebd5...a24e` 已经在 GitHub 公开仓库的历史提交里暴露，**必须作废**：

1. 打开 [阿里云百炼控制台 → API-KEY 管理](https://bailian.console.aliyun.com/?apiKey=1)
2. **删除**旧的 Key
3. **新建**一个 Key，复制下来（下一步要用），**不要再粘贴到任何前端代码里**

---

## 第 2 步：部署 Edge Function 代理

在项目目录下执行（需先安装 [Supabase CLI](https://supabase.com/docs/guides/cli)）：

```bash
# 安装 CLI（macOS）
brew install supabase/tap/supabase

# 登录并关联你的项目
supabase login
supabase link --project-ref skxpfgqylfpavoiurfhr

# 把新 Key 存为服务端密钥（替换 sk-新Key）
supabase secrets set DASHSCOPE_API_KEY=sk-新Key

# 部署代理函数
supabase functions deploy ai-proxy
```

部署成功后可以验证（应返回 `{"used":0,"limit":35}` 之类）：

```bash
curl -s "https://skxpfgqylfpavoiurfhr.supabase.co/functions/v1/ai-proxy" \
  -H "Authorization: Bearer <你的anon key>" \
  -H "Content-Type: application/json" \
  -d '{"service":"quota"}'
```

---

## 第 3 步：执行数据库 SQL（RLS + 额度表 + 存储策略）

1. 打开 Supabase 控制台 → **SQL Editor**
2. 把 `supabase/setup.sql` 的内容整段粘贴进去 → **Run**
3. 再到 **Storage → interview-videos → Configuration**，确认 **Public bucket 开关已关闭**（SQL 里也改了，控制台再核对一遍）

执行后效果：
- `interview_records` 开启 RLS，任何人只能读写自己的记录
- `interview-videos` 变成私有桶，旧的公开链接全部失效，回放走 1 小时签名链接
- 新增 `ai_usage` 额度表（只有服务端能访问）

> 注意：私有化后，**旧记录里存的公开链接会 404**（这正是我们要的效果——它们本来就泄露了）。代码已兼容：新记录存路径并自动签名回放。

---

## 第 4 步：推送前端代码到 GitHub Pages

```bash
git add -A
git commit -m "P0安全改造：API Key服务端化+每日额度+云端转写+私有视频桶+RLS"
git push
```

> 提示：旧 Key 还留在 git 历史里，但因为第 1 步已经把它作废了，所以不用清洗历史。

---

## 本次改了什么（对照 P0 清单）

| 问题 | 解决方式 |
|---|---|
| **P0-1** Key 明文暴露 | 新增 `supabase/functions/ai-proxy`（Key 只存服务端环境变量）+ `ai-client.js` 统一前端调用；4 个页面的明文 Key 全部删除 |
| **P0-2** 国内 Chrome 语音识别不可用 | 面试页每次回答同步录音，浏览器识别失败时自动把录音发给 `qwen3-asr-flash` 云端转写（base64 直传，最长 5 分钟/题）；配置页设备检测不再因此阻断 |
| **P0-3** 录像公开桶 | 改私有桶 + 按 `{user_id}/` 目录的存储策略；上传只存路径，报告页回放时签发 1 小时签名链接（兼容旧 http 链接） |
| **P0-4** 云端数据未隔离 | `setup.sql` 开启 RLS（select/insert/delete 仅限本人）；历史成长页、个人中心查询同时加了显式 `eq('user_id')` 双保险 |
| **P0-5** 免费额度只是文案 | 代理按"额度单位"计费扣减：匿名（按 IP）35 单位/天 ≈ 1 场，登录用户 105 单位/天 ≈ 3 场；超限返回 429 友好提示；配置页顶部改为显示**真实**剩余场次，登录用户可见"登录可提额"激励 |

## 额度规则速查（以后做会员体系时调这里）

- 计费单位：出题/评分 1、语音合成 1、录音转写 1、图片理解 2
- 修改限额：编辑 `supabase/functions/ai-proxy/index.ts` 顶部的 `DAILY_LIMIT_ANON` / `DAILY_LIMIT_USER`，重新 `supabase functions deploy ai-proxy` 即可
- 会员逻辑预留：代理里已区分登录/匿名身份，将来加会员表后在函数里按 `user_id` 查会员等级换限额即可
